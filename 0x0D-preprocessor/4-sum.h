#ifndef SUM_H
#define SUM_H

/*
 * File: 4-sum.h
 * Desc:  function-like macro SUM(x, y) that computes the sum
 *        of the numbers x and Header file that defines a
 *        function-like macro SUM(x, y).
 */

#define SUM(x, y) ((x) + (y))

#endif
