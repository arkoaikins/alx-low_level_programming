#include "main.h"

/**
 * mul - Multiples two integers.
 * @a:first integer
 * @b:second integer
 *
 * Return: (a*b) which is the result of the multiplication.
 */
int mul(int a, int b)
{
	return (a * b);
}
