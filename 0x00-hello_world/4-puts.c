#include <stdio.h>

/**
 * main - is the entry point of the program.
 * puts: Print its argument followed by a new line
 *        automatically,unline printf that needs \n.
 * Return: return 0 if sucessful else returni 1.
 * the \ in the puts is an escape character that
 * indicates that the next double quote is not the
 * end of the string.
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
